- [x] Fix Vite build error: update index.html to use Vite-managed entry (correct script tag) so build assets resolve with configured `base`.
- [ ] Run `npm run build` locally to verify the fix.
- [ ] Run `npm run preview` (optional) to sanity-check asset loading.


