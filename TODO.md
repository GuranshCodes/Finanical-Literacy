- [ ] Move the original FlowLedger rows (cash movements) into ExpenseBreakdown, so the two sections no longer overlap visually/data-wise.
- [ ] Redesign FlowLedger to only show the “remaining salary” split narrative you provided (investments/growth → chequing), keeping it visually distinct.
- [ ] Update Home/anchors if needed.
- [ ] Run `npm run lint` and `npm run build`


