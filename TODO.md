- [x] Gathered repo context (main render/auth/app structure, routing)
- [x] Verified likely base-path mismatch between Vite `base` and React Router
- [x] Updated `src/App.jsx` to add React Router `basename="/Finanical-Literacy"` so GitHub Pages routing works
- [ ] Run `npm run dev` and confirm the page renders (no blank/black screen)
- [ ] Run `npm run build` and `npm run preview` to verify on the built output

